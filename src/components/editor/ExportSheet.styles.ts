// DEAD FILE — see ExportSheet.tsx in this same folder for why. Safe to
// delete by hand; kept only because this session cannot delete files
// on the linked device.
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({});
